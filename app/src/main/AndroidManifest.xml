<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.snooker4real.playlistyoutube">

    <application
        android:allowBackup="true"
        android:icon="@drawable/video_player"
        android:label="@string/app_name"
        android:roundIcon="@drawable/video_player"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlaylistYoutube">
        <activity
            android:name=".DetailsVideoActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AddVideoActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>